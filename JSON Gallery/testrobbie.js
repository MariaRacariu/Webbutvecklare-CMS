// let div = '<div class="container"><h1 class="title">This is the title</h1><img src="${image.url}"></div>';
// $(div).append(div);

let name = "Maria";

let test = `<div class="container" color="red">
<p>${name}</p>
</div>`;

document.write(test);